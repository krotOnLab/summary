from typing import Protocol, Any


class LoggerProtocol(Protocol):
    """
    Протокол стандартного логгера.

    Определяет минимальный интерфейс для логгера, поддерживающего
    основные уровни логирования. Реализация должна обеспечивать
    запись сообщений с возможностью форматирования и передачи
    дополнительных параметров.

    Notes
    -----
    Этот протокол позволяет использовать любую реализацию логгера,
    совместимую с интерфейсом стандартной библиотеки logging,
    включая кастомные логгеры или mock-объекты для тестирования.

    Examples
    --------
    >>> class SimpleLogger:
    ...     def debug(self, msg: str, *args, **kwargs) -> None:
    ...         print(f"DEBUG: {msg % args if args else msg}")
    ...     # ... другие методы ...
    >>>
    >>> logger: LoggerProtocol = SimpleLogger()
    >>> logger.info("Processing %d items", 42)
    """
    
    def debug(self, msg: str, *args, **kwargs) -> Any:
        """
        Записывает сообщение уровня DEBUG.

        Используется для детальной диагностики и отладочной информации.

        Parameters
        ----------
        msg : str
            Форматирующая строка сообщения.
        *args : tuple
            Позиционные аргументы для форматирования строки.
        **kwargs : dict
            Именованные аргументы для дополнительных параметров логгера
            (например, extra, exc_info).
        """
        ...
        
        
    def info(self, msg: str, *args, **kwargs) -> Any:
        """
        Записывает сообщение уровня INFO.

        Используется для подтверждения нормальной работы приложения.

        Parameters
        ----------
        msg : str
            Форматирующая строка сообщения.
        *args : tuple
            Позиционные аргументы для форматирования строки.
        **kwargs : dict
            Именованные аргументы для дополнительных параметров логгера.
        """
        ...
        
        
    def warning(self, msg: str, *args, **kwargs) -> Any:
        """
        Записывает сообщение уровня WARNING.

        Используется для указания на потенциальные проблемы,
        которые не приводят к остановке работы.

        Parameters
        ----------
        msg : str
            Форматирующая строка сообщения.
        *args : tuple
            Позиционные аргументы для форматирования строки.
        **kwargs : dict
            Именованные аргументы для дополнительных параметров логгера.
        """
        ...
        
        
    def error(self, msg: str, *args, **kwargs) -> Any:
        """
        Записывает сообщение уровня ERROR.

        Используется для фиксации ошибок, которые влияют на текущую операцию,
        но не приводят к полной остановке приложения.

        Parameters
        ----------
        msg : str
            Форматирующая строка сообщения.
        *args : tuple
            Позиционные аргументы для форматирования строки.
        **kwargs : dict
            Именованные аргументы для дополнительных параметров логгера.
        """
        ...
        
        
    def critical(self, msg: str, *args, **kwargs) -> Any:
        """
        Записывает сообщение уровня CRITICAL.

        Используется для фиксации серьёзных ошибок, угрожающих работоспособности
        приложения или требующих немедленного вмешательства.

        Parameters
        ----------
        msg : str
            Форматирующая строка сообщения.
        *args : tuple
            Позиционные аргументы для форматирования строки.
        **kwargs : dict
            Именованные аргументы для дополнительных параметров логгера.
        """
        ...